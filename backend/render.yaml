services:
  - type: web
    name: urban-sight-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python train_v1.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
